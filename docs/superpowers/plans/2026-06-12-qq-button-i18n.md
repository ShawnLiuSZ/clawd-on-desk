# QQ 卡片按钮文字本地化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 QQ 机器人卡片**自己生成的按钮文字**（Allow/Deny、建议按钮、多选完成按钮）跟随 app 的 `lang` 设置本地化。

**Architecture:** 新增纯数据模块 `src/qq-bot-i18n.js`（按语言的卡片串 + `qqCardText(lang,key,params)`）。`createQQBotClient` 新增 `getLang` 回调（沿用 `i18n.js` 的 `createTranslator(getLang)` 模式），client 内用 `tr(key,params)` 翻译按钮文案。`main.js` 创建 client 时传 `getLang: () => lang`。正文提示、确认回执、选项按钮不在范围内。

**Tech Stack:** Node.js，`node:test` + `node:assert`，无新依赖。

参考设计：`docs/superpowers/specs/2026-06-12-qq-button-i18n-design.md`
allow/deny 复用桌面气泡的既有译法（`src/bubble-renderer.js`）：zh `批准/拒绝`、zh-TW `允許/拒絕`、ko `허용/거부`、ja `許可/拒否`。

测试命令：`node --test test/qq-bot-i18n.test.js test/qq-bot-client.test.js test/qq-approval.test.js`

---

## File Structure

- **Create** `src/qq-bot-i18n.js` — 卡片按钮串池 + `qqCardText(lang, key, params)`，纯数据，无依赖。
- **Create** `test/qq-bot-i18n.test.js` — 模块单测。
- **Modify** `src/qq-bot-client.js` — `createQQBotClient` 取 `getLang`；`buildApprovalCard`、`buildSuggestionLabel`、`buildMultiSelectElicitationCard` 用 `tr(...)`。
- **Modify** `test/qq-bot-client.test.js` — 新增多语言断言；更新受影响的既有断言。
- **Modify** `src/main.js` — `getOrCreateQQBotClient` 传 `getLang: () => lang`。

---

## Task 1: 翻译模块 `qq-bot-i18n.js`

**Files:**
- Create: `src/qq-bot-i18n.js`
- Test: `test/qq-bot-i18n.test.js`

- [ ] **Step 1: 写失败测试**

创建 `test/qq-bot-i18n.test.js`：

```js
"use strict";

const { describe, it } = require("node:test");
const assert = require("node:assert");
const { qqCardText, QQ_CARD_STRINGS } = require("../src/qq-bot-i18n");

describe("qq-bot-i18n: qqCardText", () => {
  it("returns the string for a known lang + key", () => {
    assert.strictEqual(qqCardText("zh", "allow"), "批准");
    assert.strictEqual(qqCardText("en", "deny"), "Deny");
    assert.strictEqual(qqCardText("ja", "allow"), "許可");
  });

  it("falls back to en for an unknown lang", () => {
    assert.strictEqual(qqCardText("xx", "allow"), "Allow");
  });

  it("falls back to the key itself for an unknown key", () => {
    assert.strictEqual(qqCardText("zh", "nope"), "nope");
  });

  it("substitutes {tool} / {mode} / {n} placeholders", () => {
    assert.strictEqual(qqCardText("en", "alwaysDenyTool", { tool: "Write" }), "Always deny Write");
    assert.strictEqual(qqCardText("zh", "alwaysDenyTool", { tool: "Write" }), "始终拒绝 Write");
    assert.strictEqual(qqCardText("en", "modePrefix", { mode: "ask" }), "Mode: ask");
    assert.ok(qqCardText("zh", "submitDone", { n: 3 }).includes("3"));
  });

  it("covers all five supported languages for core keys", () => {
    for (const lang of ["en", "zh", "zh-TW", "ko", "ja"]) {
      assert.ok(QQ_CARD_STRINGS[lang], `lang ${lang} present`);
      assert.ok(QQ_CARD_STRINGS[lang].allow, `lang ${lang} has allow`);
      assert.ok(QQ_CARD_STRINGS[lang].deny, `lang ${lang} has deny`);
    }
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test test/qq-bot-i18n.test.js`
Expected: FAIL —— `Cannot find module '../src/qq-bot-i18n'`。

- [ ] **Step 3: 实现模块**

创建 `src/qq-bot-i18n.js`：

```js
"use strict";

// ── QQ card button-text i18n ──
// Small, self-contained string pool for the QQ bot card BUTTON labels only.
// Kept separate from src/i18n.js (the menu/settings pool) so the pure-Node
// qq-bot-client stays focused. Values may contain {tool}/{mode}/{n} placeholders.

const QQ_CARD_STRINGS = {
  en: {
    allow: "Allow",
    deny: "Deny",
    alwaysAllow: "Always allow",
    alwaysDenyTool: "Always deny {tool}",
    alwaysTool: "Always {tool}",
    alwaysDeny: "Always deny",
    autoEdits: "Auto edits",
    planMode: "Plan mode",
    modePrefix: "Mode: {mode}",
    submitDone: "Done ({n} selected)",
  },
  zh: {
    allow: "批准",
    deny: "拒绝",
    alwaysAllow: "始终允许",
    alwaysDenyTool: "始终拒绝 {tool}",
    alwaysTool: "始终允许 {tool}",
    alwaysDeny: "始终拒绝",
    autoEdits: "自动编辑",
    planMode: "计划模式",
    modePrefix: "模式：{mode}",
    submitDone: "完成（已选 {n}）",
  },
  "zh-TW": {
    allow: "允許",
    deny: "拒絕",
    alwaysAllow: "一律允許",
    alwaysDenyTool: "一律拒絕 {tool}",
    alwaysTool: "一律允許 {tool}",
    alwaysDeny: "一律拒絕",
    autoEdits: "自動編輯",
    planMode: "計畫模式",
    modePrefix: "模式：{mode}",
    submitDone: "完成（已選 {n}）",
  },
  ko: {
    allow: "허용",
    deny: "거부",
    alwaysAllow: "항상 허용",
    alwaysDenyTool: "항상 {tool} 거부",
    alwaysTool: "항상 {tool} 허용",
    alwaysDeny: "항상 거부",
    autoEdits: "자동 편집",
    planMode: "플랜 모드",
    modePrefix: "모드: {mode}",
    submitDone: "완료 ({n}개 선택)",
  },
  ja: {
    allow: "許可",
    deny: "拒否",
    alwaysAllow: "常に許可",
    alwaysDenyTool: "常に {tool} を拒否",
    alwaysTool: "常に {tool} を許可",
    alwaysDeny: "常に拒否",
    autoEdits: "自動編集",
    planMode: "プランモード",
    modePrefix: "モード: {mode}",
    submitDone: "完了（{n}件選択）",
  },
};

function qqCardText(lang, key, params) {
  const dict = QQ_CARD_STRINGS[lang] || QQ_CARD_STRINGS.en;
  let s = dict[key];
  if (s == null) s = (QQ_CARD_STRINGS.en[key] != null ? QQ_CARD_STRINGS.en[key] : key);
  if (params && typeof s === "string") {
    s = s.replace(/\{(\w+)\}/g, (m, name) =>
      Object.prototype.hasOwnProperty.call(params, name) ? String(params[name]) : m);
  }
  return s;
}

module.exports = { QQ_CARD_STRINGS, qqCardText };
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node --test test/qq-bot-i18n.test.js`
Expected: PASS（5 个用例全绿）。

- [ ] **Step 5: 提交**

```bash
git add src/qq-bot-i18n.js test/qq-bot-i18n.test.js
git commit -m "feat(qqbot): add QQ card button-text i18n string pool"
```

---

## Task 2: client 按 `getLang` 本地化按钮

**Files:**
- Modify: `src/qq-bot-client.js`（顶部 require；`createQQBotClient` 内取 `getLang` + `tr`；`buildSuggestionLabel`；`buildApprovalCard` 的 Allow/Deny；`buildMultiSelectElicitationCard` 的提交按钮）
- Test: `test/qq-bot-client.test.js`

- [ ] **Step 1: 写失败测试 + 更新受影响的既有断言**

**(a) 更新既有断言。** 当前多选用例（`describe("qq-bot-client: buildElicitationCard")` 内 "multi-select: marks selected options and adds a submit button"）断言提交按钮含 `已选 2`。本地化后默认 client 走 en（"Done (2 selected)"），该断言需改为显式 zh。把该用例里这一行：

```js
    assert.ok(submit.render_data.label.includes("已选 2"), "submit label shows selected count");
```

改为构造 zh client 并断言（替换整个用例的 client 构造与该断言）。即把该用例首行：

```js
    const client = createQQBotClient(makeConfig(), { httpPost: makeMockHttpPost() });
```

改为：

```js
    const client = createQQBotClient(makeConfig(), { httpPost: makeMockHttpPost(), getLang: () => "zh" });
```

该断言行改为：

```js
    assert.ok(submit.render_data.label.includes("完成") && submit.render_data.label.includes("2"),
      "submit label shows localized done + selected count");
```

**(b) 新增多语言用例。** 加到 `describe("qq-bot-client: buildApprovalCard", ...)` 块内：

```js
  it("localizes Allow/Deny buttons per getLang", () => {
    const zh = createQQBotClient(makeConfig(), { httpPost: makeMockHttpPost(), getLang: () => "zh" });
    const card = zh.buildApprovalCard("Bash", { command: "ls" }, "s", "qq_p", [], "AB");
    const labels = card.keyboard.content.rows.flatMap((r) => r.buttons).map((b) => b.render_data.label);
    assert.ok(labels.some((l) => l.includes("批准")), "allow localized to zh");
    assert.ok(labels.some((l) => l.includes("拒绝")), "deny localized to zh");
  });

  it("localizes suggestion button labels per getLang", () => {
    const zh = createQQBotClient(makeConfig(), { httpPost: makeMockHttpPost(), getLang: () => "zh" });
    const card = zh.buildApprovalCard("Bash", {}, "s", "qq_p", [
      { type: "setMode", mode: "acceptEdits" },
      { type: "addRules", behavior: "deny", toolName: "Write" },
    ], "AB");
    const labels = card.keyboard.content.rows.flatMap((r) => r.buttons).map((b) => b.render_data.label);
    assert.ok(labels.some((l) => l.includes("自动编辑")), "acceptEdits localized");
    assert.ok(labels.some((l) => l.includes("始终拒绝 Write")), "deny-rule localized");
  });

  it("defaults to English button text when no getLang is provided", () => {
    const client = createQQBotClient(makeConfig(), { httpPost: makeMockHttpPost() });
    const card = client.buildApprovalCard("Bash", {}, "s", "qq_p", [
      { type: "addRules", behavior: "allow", toolName: "Bash" },
    ], "AB");
    const labels = card.keyboard.content.rows.flatMap((r) => r.buttons).map((b) => b.render_data.label);
    assert.ok(labels.some((l) => l.includes("Allow")), "default allow is English");
    assert.ok(labels.some((l) => l.includes("Always Bash")), "default suggestion is English");
  });
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test test/qq-bot-client.test.js`
Expected: FAIL —— `localizes Allow/Deny`、`localizes suggestion` 失败（当前硬编码英文 + 中文混用，且 client 不接受 getLang）。

- [ ] **Step 3: 顶部 require 翻译模块**

在 `src/qq-bot-client.js` 顶部（`const { URL } = require("url");` 之后）加：

```js
const { qqCardText } = require("./qq-bot-i18n");
```

- [ ] **Step 4: `createQQBotClient` 取 `getLang` 并建 `tr`**

在 `createQQBotClient` 函数体开头（`const logFn = ...` 附近、与其它 options 取值相邻处）加：

```js
  const getLang = typeof options.getLang === "function" ? options.getLang : () => "en";
  const tr = (key, params) => qqCardText(getLang(), key, params);
```

- [ ] **Step 5: `buildSuggestionLabel` 改用 `tr`**

把 `buildSuggestionLabel` 的英文字面量替换为 `tr(...)`（保留"自带 label 优先"逻辑）：

```js
  function buildSuggestionLabel(s) {
    if (!s || typeof s !== "object") return "";
    if (s.label && typeof s.label === "string" && s.label.trim()) return s.label.trim();
    if (s.type === "setMode") {
      if (s.mode === "acceptEdits") return tr("autoEdits");
      if (s.mode === "plan") return tr("planMode");
      const mode = mdSafe(s.mode || "");
      return mode ? tr("modePrefix", { mode }) : "";
    }
    if (s.type === "addRules") {
      const rules = Array.isArray(s.rules) ? s.rules : [s];
      const first = rules.find((rule) => rule && typeof rule === "object") || {};
      const behavior = mdSafe(s.behavior || first.behavior || "allow");
      const isDeny = behavior === "deny";
      const toolName = mdSafe(first.toolName || s.toolName || "");
      if (toolName) return isDeny ? tr("alwaysDenyTool", { tool: toolName }) : tr("alwaysTool", { tool: toolName });
      return isDeny ? tr("alwaysDeny") : tr("alwaysAllow");
    }
    return "";
  }
```

- [ ] **Step 6: `buildApprovalCard` 的 Allow/Deny 用 `tr`**

把审批卡的两个固定按钮：

```js
    const buttons = [
      makeButton("1", "✅ Allow", permId, "allow", 1),
      makeButton("2", "❌ Deny", permId, "deny", 0),
    ];
```

改为：

```js
    const buttons = [
      makeButton("1", `✅ ${tr("allow")}`, permId, "allow", 1),
      makeButton("2", `❌ ${tr("deny")}`, permId, "deny", 0),
    ];
```

- [ ] **Step 7: 多选提交按钮用 `tr`**

把 `buildMultiSelectElicitationCard` 里的提交按钮：

```js
    const submitButton = makeButton(
      String(buttonId++),
      `✅ 完成 (已选 ${totalSelected})`,
      permId,
      "elicitation-submit",
      1
    );
```

改为：

```js
    const submitButton = makeButton(
      String(buttonId++),
      `✅ ${tr("submitDone", { n: totalSelected })}`,
      permId,
      "elicitation-submit",
      1
    );
```

- [ ] **Step 8: 运行测试确认通过（含既有不回归）**

Run: `node --test test/qq-bot-i18n.test.js test/qq-bot-client.test.js test/qq-approval.test.js`
Expected: 全绿。新多语言用例 PASS；default-en 用例 PASS；更新后的多选 zh 用例 PASS；其余既有用例不回归（它们不传 getLang → 走 en → 英文断言如 `Always Bash`/`Allow` 仍成立）。

- [ ] **Step 9: 提交**

```bash
git add src/qq-bot-client.js test/qq-bot-client.test.js
git commit -m "feat(qqbot): localize card button text via getLang"
```

---

## Task 3: main.js 注入 `getLang`

**Files:**
- Modify: `src/main.js`（`getOrCreateQQBotClient`，约 1141 行 `createQQBotClient(normalized, { log: console.log, WebSocket })`）

- [ ] **Step 1: 传入 getLang**

把 `getOrCreateQQBotClient` 里的：

```js
  _qqBotClient = createQQBotClient(normalized, { log: console.log, WebSocket });
```

改为（`lang` 是 main.js 顶层已有的可变变量，`main.js:318` 定义、`2867` setter 更新；回调每次读最新值）：

```js
  _qqBotClient = createQQBotClient(normalized, { log: console.log, WebSocket, getLang: () => lang });
```

- [ ] **Step 2: 语法校验**

Run: `node --check src/main.js`
Expected: 无输出（语法 OK）。main.js 依赖 Electron 无法单测；运行时正确性由 Task 1/2 单测 + Task 4 真机验证覆盖。

- [ ] **Step 3: 提交**

```bash
git add src/main.js
git commit -m "feat(qqbot): pass app language into QQ bot client"
```

---

## Task 4: 真机验证（可选，app 关闭时）

- [ ] **Step 1: 用注入 getLang 的脚本发卡片**

复用 `scripts/qq-multiselect-live-test.js` 的连接模式，临时在创建 client 时加 `getLang: () => "zh"`（或读真实设置），发一张审批卡 + 一张多选卡，人工确认：
1. 审批卡按钮为「批准 / 拒绝」、建议按钮为中文（如「自动编辑」「始终拒绝 Write」）。
2. 多选完成按钮为「完成（已选 N）」。
3. 把 `getLang` 改成 `() => "en"` 再发一次，确认变回英文。

---

## Self-Review

**Spec coverage：**
- 范围只按钮（Allow/Deny + 建议 + 完成）→ Task 2 Step 5/6/7 ✅
- 语言 en/zh/zh-TW/ko/ja + 回退 → Task 1 模块 + 测试 ✅
- 数据流 getLang 回调（运行时切换）→ Task 2 Step 4 + Task 3 ✅
- 翻译模块独立于 i18.js → Task 1 新建 `qq-bot-i18n.js` ✅
- 不动正文/确认/选项按钮 → 计划未触及这些代码 ✅
- emoji 与文案分离 → `✅/❌/📋` 仍在 client 内拼接，不进翻译表 ✅
- allow/deny 复用气泡译法 → Task 1 字符串采用 `批准/拒绝` 等 ✅

**Placeholder scan：** 无 TBD/TODO；每步含完整代码。Task 4 标注为可选真机验证。

**Type consistency：** 翻译键名（allow/deny/alwaysAllow/alwaysDenyTool/alwaysTool/alwaysDeny/autoEdits/planMode/modePrefix/submitDone）在 `qq-bot-i18n.js`、`buildSuggestionLabel`、提交按钮三处一致；占位符 `{tool}`/`{mode}`/`{n}` 与 `qqCardText` 替换逻辑一致；`getLang`/`tr` 签名一致；既有 submit 断言已在 Task 2 Step 1(a) 同步更新，避免回归冲突。
